extends layout

block content
  header
    h1 Computer vision services
  .container-fluid
    .row
      .col
        div(id='camera')
        div(id='result')
        button(id='upload-image', class='btn btn-primary') Process 
          i(class='fa fa-cog', aria-hidden='true')

block append javascript_internal
  script(src='/javascripts/internal/camera.js', type='text/javascript')
